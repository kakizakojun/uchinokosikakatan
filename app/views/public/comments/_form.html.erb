
<%= form_with(model: [post, comment], local: false) do |f| %>
  <div>
    <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", class: 'comment-area form-control' %>
  </div>
  <div class="text-right">
    <%= f.submit "送信する", class: 'btn btn-info' %>
  </div>
<% end %>