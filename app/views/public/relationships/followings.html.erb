<div class="container">
  <div class="row py-5">
    <h2 class="font-weight-bold text-secondary">フォロー一覧</h2>
  </div>
  <div class="row">
    <% if @users.exists? %>
      <%= render 'public/users/index', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>

